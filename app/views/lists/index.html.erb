<%= render "shared/banner", name: "Organize your movies", image: "banner.jpg" %>


<div class="container">
  <div class="cards my-3">
    <% @lists.each do |list| %>
      <%= render "shared/card_category", name: list.name, image_url: list.image_url %>
    <% end %>
  </div>
</div>
