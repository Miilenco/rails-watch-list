<h1>Movies in <%= @list.name %> list</h1>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li>
      <%= bookmark.movie.title %> -
      <%= link_to "<i class='fa-solid fa-trash-can'></i>".html_safe, bookmark_path(bookmark), data: { turbo_method: :delete } %>
    </li>
  <% end %>
</ul>
